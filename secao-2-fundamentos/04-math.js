const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2); 

console.log(area); //armazenado na variável área, o calculo feito na constante acima.
console.log(area.toFixed(2));
console.log(typeof Math); //lembrando que o Math não é uma função, o tipo dele é objeto.