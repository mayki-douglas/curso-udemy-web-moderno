//usamos var para chammar as variáveis e demos os nomes abaixo.

var nome = "Caneta";
var quantidade = 28;
var preco = 6.5;
var imposto = 2.1;
var precoFinal = preco + imposto; //em Javascript, se os valores forem númericos, a soma pode ser feita normalmente.

//usamos o console e chamamos o nome da variável para imprimir o resultado.
console.log(nome);
console.log(quantidade);
console.log(preco);
console.log(imposto);
console.log(precoFinal); //resultado da soma do preço final, chamando as variáveis preço e imposto.